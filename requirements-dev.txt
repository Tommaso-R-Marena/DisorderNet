# Development / CI test dependencies
pytest>=8.0
pytest-cov>=5.0
ruff>=0.6
torch>=2.1
numpy>=1.24
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.13
requests>=2.31
tqdm>=4.66

# The CPU pipeline / predictor tests also need the runtime deps in
# requirements-cpu.txt (lightgbm, xgboost, fair-esm). CI installs both.
