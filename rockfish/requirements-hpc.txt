# DisorderNet HPC (Rockfish / Slurm) — install into a venv after loading CUDA
#
# Setup (once):
#   module purge
#   module load gcc/11.4.0 cuda/11.8.0   # versions may vary — check `module spider cuda`
#   python -m venv ~/venvs/disordernet
#   source ~/venvs/disordernet/bin/activate
#   pip install -r rockfish/requirements-hpc.txt
#
# PyTorch: install the CUDA wheel matching your cluster driver, e.g.:
#   pip install torch --index-url https://download.pytorch.org/whl/cu118

fair-esm==2.0.0
scikit-learn>=1.3,<2
matplotlib>=3.7
seaborn>=0.13
requests>=2.31
biopython>=1.83
tqdm>=4.66
pandas>=2.0
lightgbm>=4.0
xgboost>=2.0
numpy>=1.24
