# DisorderNet CPU pipeline dependencies (README "Option 2 / Path A").
#
# These packages are needed to run the local CPU pipeline end-to-end:
#   python fetch_disprot.py
#   python extract_esm_embeddings.py
#   python run_v6_mem.py
#   python generate_figures_v6.py
#
# Install (into a venv):
#   pip install -r requirements-cpu.txt
#
# For the test suite only, use requirements-dev.txt instead (no ESM/GBDT needed).

torch>=2.1
numpy>=1.24
scikit-learn>=1.3
lightgbm>=4.0
xgboost>=2.0
fair-esm==2.0.0
matplotlib>=3.7
seaborn>=0.13
requests>=2.31
tqdm>=4.66
