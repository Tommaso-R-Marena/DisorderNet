# Supplementary Information

**A benchmark cannot order more methods than its labels can distinguish:
capacity limits in CAID, and the protocol that escapes them**

Tommaso R. Marena

---

## Contents

| | |
|---|---|
| **Note S1** | The capacity theorem: statement, proof outline, and scope |
| **Note S2** | The second-order identity, its hypotheses, and two corrections |
| **Note S3** | The protocol, worked, including what it refuses to order |
| **Note S4** | Pre-registered variants and their outcomes — five negatives |
| **Note S5** | Errors found and corrected, with the measurement that caught each |
| **Note S6** | Inventory of the formal development |
| **Note S7** | The screen: arbitrary dependence, and the unit of testing |
| **Note S19** | Is the bound tight? Measured, and the scope condition it implies |
| **Table S1** | AUC decomposition, every method × every reference (CSV, 430 rows) |
| **Table S2** | Inversion certificates against the pooled winner (CSV) |
| **Table S3** | Pairwise leaderboard, complete, five references (CSV, 357 rows) |
| **Table S4** | Operating cost, whole field, three tolerated miss rates (CSV, 1,290 rows) |
| **Table S5** | All thirty per-target paired tests with Holm adjustment (CSV) |
| **Table S6** | Capacity by benchmark, three independent routes (CSV) |
| **Table S7** | Per-protein label and pairwise noise from MobiDB (CSV, 159 rows) |
| **Table S8** | Cross-round reproducibility, all protocol pairings (CSV) |
| **Table S9** | Coverage as a confound — what declining targets is worth |
| **Table S10** | Thresholded error counts at each method's own operating point |
| **Table S11** | Certification frontier against assumed annotation error |
| **Table S12** | Data provenance: md5s and composition |
| **Table S13** | Job registry |
| **Table S14** | Registered endpoints closed for this paper (CSV) |
| **Table S15** | The region-screen instantiation, both rounds (CSV) |
| **Table S16** | The screen as run: discoveries, FDP, power, calibration budget (CSV) |
| **Table S17** | The capacity bound instantiated on ten machine-learning benchmarks |
| **Table S18** | The escape measured on ImageNet, CIFAR and MNIST (CSV) |

CSV tables are in `paper/supplementary/` and are regenerated by
`paper/make_supplementary.py` directly from the analysis outputs.

---

## Note S1 — The capacity theorem

### Statement

Let a benchmark score `n` items, each method receiving a score that is an
average over those items and therefore lies on the grid of denominator `n`. Let
the annotation be wrong on at most a fraction `ε` of items (a worst-case budget,
not a probability model), and let `δ ≥ 0` be the smallest score difference worth
calling a difference. Write `c = max(δ, 2ε)`. Then any family of methods that
the benchmark places in a *certified* total order has cardinality at most

    k(n, ε, δ) = n / (⌊c·n⌋ + 1) + 1.

`BenchmarkCapacity.card_le_benchCapacity`, Lean 4, sorry-free, axioms
`propext`, `Classical.choice`, `Quot.sound`.

**Sharpness.** `benchCapacity_attained` constructs a family of exactly `k`
methods that the benchmark does resolve, so `k` is the capacity and not merely
an upper bound. The theorem and its attainment instance are given in three score
models — values on the grid `{0, 1/n, …, 1}`, integer error counts in `{0,…,N}`,
and real scores in `[0, R]` — because the first is what CAID reports, the second
is what a thresholded metric reports, and the third is what a referee will ask
about.

**The n-free corollary.** `benchCapacity_noise_only`: with `δ = 0`,

    k ≤ ⌈1/(2ε)⌉    for every n.

Disorder-PDB has 233 targets and Disorder-NOX 178; both give 7. CAID2's
Disorder-PDB has 256 and also gives 7. **Collecting targets does not buy
resolution the labels lack.**

### Proof outline

The scores of a certified family must be pairwise separated by more than the
noise can move them, which is `c` in the units of the score. On a grid of
denominator `n`, values separated by more than `c` are separated by at least
`⌊c·n⌋ + 1` grid steps, and `[0, 1]` contains `n` steps. Counting the maximum
number of points one can place on `n` steps at that spacing gives the bound; the
attainment instance places them exactly at the spacing.

### The converse, which is what makes this impossibility rather than low power

`unresolvable_pair`. A benchmark observes the **annotation**, not the truth.
Every labelling within the noise budget of the annotation is a consistent
truth. For two methods whose measured scores differ by less than the budget, the
proof *constructs two such truths* — one under which method A is strictly better
and one under which B is — both compatible with every observation the benchmark
recorded.

The ordering is therefore **not a function of the data**. This is the reason no
amount of resampling helps: a bootstrap, a permutation test, leave-one-out and
Monte Carlo all estimate the sampling distribution of a statistic computed from
the same observations, and the ordering is not determined by those observations.
More resamples shrink the Monte Carlo error in locating a p-value and leave the
p-value itself alone. **Only new data or better labels move this bound.**

`over_capacity_has_close_pair` closes the loop: once the entry list exceeds the
capacity, a pair inside the budget necessarily exists. With 117 entrants and a
capacity of 7 on marginal scores, CAID3 is over capacity by a factor of
seventeen — but see Note S19, which measures whether the worst case is reachable
and finds a paired capacity of 18, so the honest factor for a *leaderboard
comparison* is 6.5.

### Scope, stated rather than assumed

- The noise model is a **worst-case budget**, not a probability model. That is
  precisely why the converse is a hard impossibility rather than a power
  statement; a stochastic model would give a different and weaker theorem.
- "At capacity" is the maximum size of a **certifiable family**. It is never
  promoted to a claim that such a family has been certified.
- `ε = 0.0801` is measured on 160 of 319 Disorder-PDB targets — those with a
  usable MobiDB record — pooled over 61,013 residues. The 159 remaining targets
  are excluded from the estimate, not assumed noise-free.
- `δ = 0` throughout. Any non-zero effect-size threshold only lowers capacity.
- The theorem bounds what a benchmark can certify about **error-count or
  averaged-score** rankings. It does not bound a *paired* ranking statistic,
  which is the loophole Note S2 exploits deliberately and openly.

---

## Note S2 — The second-order identity

### Statement

Let `T ⊆ ι` be the truth on one protein (the disordered residues) and `L ⊆ ι`
the annotation. A pair `(p, q)` with `p ∈ T`, `q ∉ T` is **discordant** if the
annotation orders it the other way: `p ∉ L` and `q ∈ L`. Write `d = |T \ L|`
(down-flips: truly disordered, annotated ordered) and `u = |L \ T|` (up-flips).
Then, with no hypotheses at all:

    discordant(T, L) = 2 · d · u        `discordant_eq_flip_product`
    noise(T, L)      = d + u            `noise_eq_flip_add`
    discordant       ≤ noise² / 2       `discordant_le_noise_sq`

with equality in the third exactly when the flip classes balance
(`discordant_eq_of_balanced`, `d = u`); `sharp_instance` realises it on four
residues.

The pairs a pairwise protocol can score are the ones **both** labellings order,
and they are counted exactly too:

    |comparable(T, L)| = 2 · (|T ∩ L| · |(T ∪ L)ᶜ| + d · u)   `card_comparablePairs`

so the pair noise rate is exactly

    ν_pair = d·u / (|T ∩ L| · |(T ∪ L)ᶜ| + d·u).

`noise_orderKey` shows this quantity **is** the label noise of the answer key
the annotation induces on those pairs, which is what lets the residue-level
capacity machinery apply at the pair level verbatim: `pairwise_capacity_bound`
gives a certified family of at most `max 1 ⌈1/(2 ν_pair)⌉` members, with no
hypotheses.

### The closed form, and its hypotheses

    nuPair_le_two_eps_sq :        ν_pair ≤ 2 ε²
    pairwise_capacity_quadratic : ν_pair ≤ 2 ε²  ∧  ⌈1/(4 ε²)⌉ ≤ pairwiseCapacity

both require `BalancedClasses T L` — among the residues the two labellings agree
on, as many are disordered as ordered, `|T ∩ L| = |(T ∪ L)ᶜ|` — and `ε ≤ 1/4`;
the capacity half also needs both flip classes nonempty. Balance is
load-bearing, not decoration: the denominator `|T ∩ L|·|(T ∪ L)ᶜ|` collapses
when the classes are lopsided and the gain collapses with it.
`capacity_example` exhibits sixteen residues where every hypothesis holds and
the capacity is 25 against a residue capacity of 4, so the statement is not
vacuous.

### Measurement

MobiDB publishes the missing-residue call of each individual deposited structure
(`derived-missing_residues-mobi-{PDBID}_{CHAIN}`). For each protein we enumerate
all pairs of its structures and restrict to residues both cover: 159 CAID3
Disorder-PDB targets, 2,746 structure pairs.

| | pooled | median protein |
|---|---:|---:|
| `ε_label` | 0.0651 | 0.0342 |
| `ε_pair` | **0.0100** | 0.0006 |
| ratio | 6.54 | 57 |
| capacity `⌈1/2ε⌉` | **8 → 51** | |

Per-protein values for all 159 targets are in **Table S7**.

**The hypotheses of the closed form were checked, not assumed, and they fail.**

| | structure pairs | |
|---|---:|---:|
| checked | 2,746 | |
| balanced agreement classes within 10% | **54** | 2.0% |
| noise rate `ε ≤ 1/4` | 2,606 | 94.9% |
| **both hypotheses** | **32** | **1.2%** |
| bound `ν_pair ≤ 2ε²` holds | 2,248 | 81.9% |
| **… among the 32 satisfying both** | **32** | **100%** |

Pooled, the agreement classes stand at 240,506 disordered against 555,402
ordered — a ratio of 0.433 on a reference that is 31.6% disordered — and the
measured rate exceeds the closed form, 0.00996 against 0.00848. The bound holds
wherever the theorem says it holds and nowhere is it violated under its own
hypotheses. What CAID3 cannot use is the shortcut: **the gain has to be measured
on each benchmark rather than predicted from its label-noise rate.**

### Two things this got wrong, and what caught each

**The mechanism.** An earlier version attributed the gap to spatial correlation:
disorder flips in blocks, so only pairs crossing a block boundary reverse, and
the reduction should scale as `1/L`. The block statistics are real — 7,100
disagreement blocks across 147 proteins, median length 3, mass-weighted mean
73.8, 67.4% of context-dependent residues in blocks of ten or more — but
`1/73.8 = 0.0136` against a measured ratio of **0.1075** over-predicts the
reduction eightfold. The identity above needs no correlation assumption.

**The number.** An earlier measurement put `ε_pair` at 0.0070 and the capacity at
72, on a denominator that counted the pairs ordered by *either* labelling with
the discordant ones counted twice,

    2ae + 4du + (a+e)(d+u)   instead of   2(ae + du),

which is larger than the comparable set and mixes ordered with unordered counts.
Formalising `card_comparablePairs` is what exposed it: the two expressions are
not equal. On the correct denominator the rate is **1.42× larger** and the
capacity is **51**. Both numbers are kept in `relative_noise.json`
(`eps_pairwise` and `eps_pairwise_superseded`) so the correction is auditable.

## Table S17 — The capacity bound outside disorder

`card_le_benchCapacity` is stated for a benchmark whose score is an average over
`n` items on the grid `1/n`, with the annotation wrong on at most a fraction `ε`.
**Top-1 accuracy on a test set is exactly that**: flipping one label moves
accuracy by `1/n`, so a wrong fraction `ε` moves it by at most `ε`, and two
models must differ by more than `2ε` before the observation certifies their
order. No adaptation is needed.

Northcutt, Athalye and Mueller (NeurIPS 2021, arXiv:2103.14749, Table 1)
measured `ε` for ten benchmarks by running confident learning over each test set
and having every flagged item checked by human annotators. Substituting their
rates (`results/caid3/benchmark_capacity_field.py`):

| benchmark | modality | test set | ε | **capacity** | over the range published models occupy |
|---|---|---:|---:|---:|---:|
| QuickDraw | image | 50,426,266 | 10.12% | **5** | — |
| CAID3 (residue labels) | protein | 99,239 | 8.01% | **7** | — |
| CIFAR-100 | image | 10,000 | 5.85% | **9** | 3 |
| ImageNet | image | 50,000 | 5.83% | **9** | 4 |
| Amazon Reviews | text | 9,996,437 | 3.90% | 13 | — |
| IMDB | text | 25,000 | 2.90% | 18 | — |
| Caltech-256 | image | 29,780 | 1.54% | 33 | — |
| AudioSet | audio | 20,371 | 1.35% | 38 | — |
| 20news | text | 7,532 | 1.09% | 46 | — |
| CIFAR-10 | image | 10,000 | 0.54% | 93 | 9 |
| MNIST | image | 10,000 | 0.15% | 334 | 5 |
| **CAID3 (within-protein pairs)** | protein | 99,239 | **1.00%** | **50** | — |

Three things to read carefully.

**The rates are lower bounds**, since only algorithmically flagged items were
checked, so the capacities are **upper** bounds. The true capacities are
smaller.

**The last column is illustrative, not sourced.** Capacity in the theorem is
computed over the full score range `[0,1]`; published models occupy a far
narrower band, and a certified family of `k` members needs `k−1` gaps of more
than `2ε`, so the realisable count over a band of width `w` is
`⌊w/(2ε)⌋ + 1`. The bands assumed are ImageNet [0.55, 0.92], CIFAR-100
[0.65, 0.96], CIFAR-10 [0.90, 0.996], MNIST [0.985, 0.9987]; they are our
estimates of where published top-1 accuracies lie, stated so a reader can
substitute their own. The point does not depend on the exact band: any plausible
one gives a single-digit answer.

**The bound does not say a published ranking is wrong.** It says the data cannot
establish it. If label errors shifted every model equally the ordering would
survive — but that is an assumption about the errors, and Northcutt *et al.*
measured the opposite: correcting the labels reorders these leaderboards, and
systematically, with higher-capacity models faring worse once corrected. This
paper's contribution is not the observation that rankings are unstable, which is
theirs; it is that the instability has a computable bound, that the bound is an
impossibility rather than a limitation of any analysis, and that whether a
benchmark can escape it is decided by the statistic it chose.

## Table S18 — The escape, measured on ImageNet

Northcutt *et al.* released, for every algorithmically flagged item, the given
label, the proposed correction, and the votes of the human annotators who
adjudicated it. That is exactly the `(T, L)` pair the discordance identity needs
— `L` the annotation the benchmark ships, `T` the annotation after correction —
so the escape route can be measured on ImageNet rather than argued about.

The alternative statistic is the one the theorem points at: **per-class
one-vs-rest AUC, averaged over classes.** Each class is a group; within it the
metric depends only on the ranking of images by their score for that class. It
is a standard metric, it measures the same ability as top-1 accuracy, and unlike
top-1 accuracy it has the pairing structure.

Adjudication rule: an item is a corrected error when the plurality of annotators
chose the proposed label, ties resolved to the given label. Items whose
plurality was *neither* or *both* are errors with no determinate replacement;
they are counted in the label rate and, in the per-class computation, leave
their given class without joining another. Both variants are computed
(`results/caid3/imagenet_escape.py`) and differ by under a factor of two.

| benchmark | items | classes | validated errors | ε_label | ε_pair | capacity, top-1 | capacity, per-class AUC |
|---|---:|---:|---:|---:|---:|---:|---:|
| **ImageNet** | 50,000 | 1,000 | 2,561 | 0.0512 | 1.3×10⁻⁶ | **10** | **387,425** |
| CIFAR-100 | 10,000 | 100 | 421 | 0.0421 | 1.8×10⁻⁵ | 12 | 27,337 |
| CIFAR-10 | 10,000 | 10 | 29 | 0.0029 | 7.1×10⁻⁷ | 173 | 701,173 |
| MNIST | 10,000 | 10 | 13 | 0.0013 | 5.6×10⁻⁸ | 385 | 8,990,001 |

Same items, same labels, same adjudicated errors; four to five orders of
magnitude in what the benchmark can resolve, decided by the statistic alone.

**Our rule is stricter than the published one.** Resolving ties to the given
label gives 2,561 validated ImageNet errors against Northcutt *et al.*'s 2,916,
so ε_label = 0.0512 here against their 0.0583, and a top-1 capacity of 10 rather
than 9. Table S17 uses their published rates; this table uses ours, because the
pairwise computation must be internally consistent with the same adjudication.
The conclusion does not depend on the choice.

**Two limits.** *Capacity is necessary, not sufficient*: a constant predictor
has infinite capacity and no value, so the claim is only that among statistics
measuring the same ability one should prefer the one whose noise is
second-order. And on ImageNet only the capacity half can be demonstrated — no
archive of many models' per-image predictions exists, so the reproducibility of
the resulting ranking cannot be checked there. On CAID both halves are shown on
the same data: capacity 8 → 51, and the ranking reproduces on a held-out round
at ρ = 0.997. That is what repeat annotations buy, and it is why the protein
case carries the argument rather than merely illustrating it.

---

**Which statistics admit the escape.** The second-order identity needs a
*pairing structure*: a pair is reversed only when both its members are
mis-annotated, in opposite directions. An average of per-item losses has no such
structure — an adversary may place the whole error budget on the items that
matter — so accuracy-scored benchmarks have no escape and their capacity is
`⌈1/2ε⌉` full stop. A ranking statistic over within-group pairs does, and the
gain is what `ESCAPE.md` measures. The prescription for a benchmark designer is
therefore concrete: **if you want to resolve more than `⌈1/2ε⌉` methods, score
pairs within groups rather than items.**

---

## Note S3 — The protocol, worked

### Specification

As in the main text, steps 1–6. Two points deserve expansion.

**Why the unweighted mean (step 3).** `AUC_within` as it appears in the
decomposition identity is the **pair-weighted** mean of per-target AUCs —
weighted by each target's own positive×negative pair count — because the
identity does not close otherwise. Pair weighting lets a handful of long chains
carry the number. The protocol therefore scores the **unweighted** mean: one
protein, one vote. Both are calibration-invariant, since the invariance holds
target by target before any averaging; the relevant theorem is the per-target
form

```lean
theorem auc_target_strictMono_invariant (f : ℝ → ℝ) (hf : StrictMono f) :
    aucOn t (f ∘ s) = aucOn t s
```

from which the weighted and unweighted means both follow. The published theorem
`auc_within_strictMono_invariant` is stated for the pair-weighted form, so the
per-target statement is what the protocol should cite and it is pending
formalisation. This distinction is recorded rather than elided.

**Why coverage is a gate and not a covariate (step 1).** Declining a target
raises a within-protein score, because the targets methods decline are harder
(Table S9). A method that answers 87% of Linker is not scored on Linker; it is
scored on an easier benchmark with the same name.

### Worked example: what the protocol refuses to order

Disorder-PDB, 120 entered, 60 eligible, 233 two-class targets, ε_pair = 0.0100,
capacity ⌈1/(2·0.0100)⌉ = 51.

The leader, DisorderNet-pbias at 0.9512, is separated at the Holm-corrected
Wilcoxon margin from **55 of the 59** other eligible methods. The four it is not
separated from are reported as tied with it at rank 1 — not as ranks 2, 3, 4 —
and that is the whole of the protocol's honesty mechanism. The residue-level
protocol, on the same data, can certify **seven** orderings in total.

### Rank changes, five references

Full table in **Table S3**. The largest single movements:

| method | benchmark | pooled | pairwise | move |
|---|---|---:|---:|---:|
| LIPNet | Binding | 60 | 6 | **+54** |
| ProBiPred-nucleic | Binding | 62 | 17 | +45 |
| DisorderUnetLM | Binding-IDR | 67 | 23 | +44 |
| LINKER-Pred2 | Binding-IDR | 29 | 66 | **−37** |
| rawMSA | Binding | 4 | 40 | −36 |
| flDPnn2 | Binding-IDR | 64 | 29 | +35 |
| UdonPred-TriZOD | Linker | 44 | 11 | +33 |
| **AlphaFold-pLDDT** | **Disorder-NOX** | **39** | **6** | **+33** |

---

## Note S4 — Pre-registered variants and their outcomes

Every architectural idea in this project was registered before its run, with
floors, endpoints and a decision rule fixed in advance. **Five were rejected.**
They are reported here in full because the capacity result explains them, and
because a paper that reports only the variants that worked is the failure mode
this project was built to avoid.

| # | variant | registered in | primary endpoint, as registered | measured | outcome |
|---:|---|---|---|---:|---|
| 1 | **Private trunk** — detach the binding tasks from the shared trunk | PREREG_4 | Binding-IDR pooled ≥ 0.5900 | 0.5800 | **REJECTED**, plus three floors failed |
| 2 | **Backbone handedness** — two chirality channels from Cα virtual torsions | PREREG_5, _6 | A/B against a regime-matched control on the fixed holdout | null on every task | **REJECTED** — the gate opened 22× over threshold and the A/B saw nothing |
| 3 | **Motif-scale read-out** — a narrow private stack for the binding tasks | PREREG_7 | beat `mt_control` on pooled Binding-IDR | 0.5281 vs 0.5422 (−0.0141) | **REJECTED** |
| 4 | **Distribution matching** — a W₁ term on the protein-level score distribution | PREREG_8 | beat `mt_control` on **between-protein** Disorder-NOX | 0.8564 vs 0.8600 (−0.0036) | **REJECTED** |
| 5 | **Within-protein ranking loss** — optimise the calibration-invariant part directly | PREREG_9 | beat `mt_control` on **within-protein** Disorder-NOX | 0.8500 vs 0.8511 (−0.0011) | **REJECTED** |
| 6 | **Ensemble** — equal-weight rank fusion, membership chosen on the holdout | PREREG_10 | beat PUNCH2 on pooled Disorder-PDB | +0.0062, p = 0.120 | **not significant** |

Endpoints 3–5 were computed for this paper (job 30098857,
`results/caid3/registered_endpoints.py`), because each registration states its
endpoint on a statistic the CAID3 evaluator does not write and each had been
filed as rejected on the pooled score instead. **All three fail on the statistic
they were registered on, and not one of the fifteen paired per-target
comparisons against the control survives Holm** (smallest adjusted p = 0.345).

The two mechanism predictions are the sharper part. PREREG_8 said the
distribution-matching term should move the **between**-protein component,
because the term is permutation-invariant within a protein and therefore cannot
touch the within-protein ordering. PREREG_9 said the ranking surrogate should
move the **within**-protein component, tested directly rather than inferred from
a pooled number. Each registration named the quantity its own mechanism had to
move; neither moved it.

**The benchmark's verdict, checked against new data.** The capacity result says
CAID3 could not have separated these variants whatever they did, which invites
the objection that real improvements are being hidden. New data is not bounded by
CAID3's capacity, so the four scorable variants were scored on the 186 temporally
held-out chains (job 30102022), against the same regime-matched control:

| variant vs `mt_control`, 186 unseen chains | pooled | within (pair-wtd) | within (unwtd) |
|---|---:|---:|---:|
| `mt_motif` | −0.0033 | −0.0048 | −0.0075 |
| `mt_rank` | −0.0035 | −0.0047 | −0.0137 |
| `mt_wass` | −0.0046 | −0.0098 | −0.0084 |
| `mt_chiral` | −0.0086 | −0.0301 | −0.0135 |

**Not one of them beats the control, on any of the three statistics, on data
none of them saw.** There was nothing for the benchmark to resolve. This is the
outcome the capacity theorem should be read as licensing: it says when to stop
looking, and when we checked with independent data, stopping was correct.

**An unregistered finding from the same computation.** On Binding-IDR,
restricting to the 42 targets that carry both classes among evaluated residues
reverses the sign of all three comparisons: `mt_motif` −0.0141 → **+0.0480**,
`mt_wass` −0.0308 → **+0.0535**, `mt_rank` −0.0050 → **+0.0117**. Ten of the 52
targets carry one class, and a single-class target contributes no within-protein
pair at all — every pair it enters is between-protein. The whole of each
variant's apparent regression lives in targets that carry no residue-level
question. The registered endpoints still fail, because they were registered on
the pooled statistic and that is the statistic that decides them; but the reason
they fail is not that the variants are worse at the residue-level question. This
is the paper's thesis appearing where nobody put it.

The chirality result is the most instructive. The registered gate — a
training-free handedness signal must beat an achiral control by ≥0.010
within-protein — opened by a factor of twenty-two (+0.2256 on Disorder-PDB,
+0.2529 on Disorder-NOX), with the fitted direction the one polymer physics
predicts: disordered residues less right-handed (mean 0.074) than ordered
(0.211), and 61.3% of torsions right-handed overall, as L-amino-acid proteins
should give. **And the A/B against a regime-matched control was null on every
task** (Disorder-PDB +0.0028, p = 0.087; Disorder-NOX +0.0019, p = 0.893;
Binding +0.0496, p = 0.200). The signal is real and the backbone already
carries it.

**Five architectures and an ensemble, each pre-registered, all failing to
separate from PUNCH2 on pooled AUC** (+0.0043, +0.0019, +0.0035, +0.0063,
+0.0062) is exactly what a benchmark over its capacity produces. We were trying
to resolve a pair the benchmark cannot resolve. On the calibration-invariant
axis the same comparison separates on all five references (main text, Fig. 4a).

---

## Note S5 — Errors found and corrected

Kept in full, because they are the reason to believe the rest, and because each
was caught by a measurement rather than by review.

| what looked true | what was true | what caught it |
|---|---|---|
| Ten CAID3 numbers | the evaluator built the head with default dilations while the checkpoints were trained WIDE; dilation changes no weight shape, so `load_state_dict(strict=True)` accepted it silently and computed a different function at under a third of the trained context | re-deriving the receptive field from the checkpoint's own recorded flags |
| Homologues removed from the validation holdout | `homologous = set(hits)` was a set of `(query_index, subject_index, identity)` triples, so `id not in homologous` was always true; not one homologue left training while the log printed a plausible count | a log line reporting 1,157 held out against 2,538 reported homologues |
| "the head has no channel for protein-level information; the receptive field is 213 residues" | GroupNorm pools over the length axis, so the dependency span is **global**; perturbing residue 0 moves the logit at residue 400 | `lite_head.measured_dependency_span`, written to test the claim |
| pairwise discordance is low because noise is block-correlated, `≈1/L` | the mechanism is second-order, `2·d·u`; `1/L` over-predicts the reduction eightfold | measuring both the block distribution and the ratio |
| `ε_pair = 0.0070`, pairwise capacity 72 | the denominator counted the pairs ordered by *either* labelling with the discordant ones counted twice. `card_comparablePairs` counts it exactly; the rate is 1.42× larger and the capacity is **51** | formalising the denominator |
| `ε_pair ≤ 2·ε_label²`, "the bound holds and is tight to 20%" | the bound needs the *agreement* classes to balance, which holds on 32 of 2,746 structure pairs; pooled, the measured rate exceeds it. It holds on all 32 that qualify | formalising the hypotheses, then checking them |
| AlphaFold-rsa's disordered-residue coverage collapses to 0.527 and it fails the risk guarantee | artefact of scoring our models on 186 chains and the baselines on 61, with different splits; on identical chains rsa covers 0.994 and achieves the guarantee | matching the chain sets |
| every Linker method must flag 100% of the protein | `α` below `1/(n+1)`: arithmetic about *n*, not a fact about any method. Now an explicit refusal by name | the uniformity of the result |
| a dihedral implementation passes its mirror test | it was wrong twice — a 180° offset, then a sign inversion — and the mirror test passed both times | an independently written second implementation |
| a source-text test passes, so the code path works | `LEADERS[task]` is a 3-tuple unpacked as a pair; the test asserted on source text and the code raised at runtime | running it |
| p = 0.0 between two identical methods | both bootstrap tails must carry the ties | comparing a method with itself |
| a certified irreducible-error ranking | `crossed_bound_loose_unbounded`: the greedy crossed-matching certificate is loose by an unbounded factor, because the obstruction is cyclic across three proteins and the certificate is pairwise | the Lean development, which proved the negative we did not ask for |
| "we have never beaten PUNCH2 significantly", stated four times | `mt_pbias` beats PUNCH2 on pooled Disorder-PDB by +0.0083, 95% CI [+0.0010, +0.0176], p = 0.0226. The checkpoint had been filed as rejected by its own pre-registration on three other benchmarks, so its Disorder-PDB comparison stopped being quoted at all | re-reading every checkpoint's result file |

---

## Note S6 — Inventory of the formal development

Lean 4, sorry-free, axioms `propext`, `Classical.choice`, `Quot.sound`
throughout.

**Metric decomposition and invariance**
`auc_pooled_decomp` · `auc_within_strictMono_invariant` ·
`auc_pooled_shift_diff` · `inversion_requires_between_gap` ·
`auc_shift_le_ceiling` · `ceiling_gap_of_crossed_matching` ·
`exists_order_ge` · `exists_bias_of_order` · `exists_gapMatrix` ·
`feasible_iff_gap` · `Example.inversion` (worked instance, reproduced exactly by
the analysis code)

**Label noise and certification**
`LabelNoise.ranking_certified` · `RobustCertificate.certificate_under_mean_error`

**Benchmark capacity**
`BenchmarkCapacity.card_le_benchCapacity` (grid / integer / real forms) ·
`benchCapacity_attained` (three attainment instances) ·
`benchCapacity_noise_only` · `card_le_capacity_labelNoise` ·
`unresolvable_pair` · `over_capacity_has_close_pair`

**Pairwise scoring** (`DiscordantPairs.lean`)
`discordant_eq_flip_product` · `noise_eq_flip_add` · `discordant_le_noise_sq`
(and `_real`) · `discordant_eq_of_balanced` · `sharp_instance` ·
`comparablePairs` / `orderKey` / `noise_orderKey` · `card_comparablePairs` ·
`card_split` · `pairwise_capacity_bound` · `BalancedClasses` ·
`nuPair_le_two_eps_sq` · `pairwise_capacity_quadratic` ·
`capacity_at_eps_0651` · `exampleHypotheses` / `exampleCounts` /
`capacity_example`

**Multiple testing under dependence** (`DependentBH.lean`,
`DependentBHSharp.lean`, `RegionScreen.lean`)
`wgt_decomp` · `fdp_eq_sum_wgt` · `SelfConsistentP` / `bhList_selfConsistent` ·
`mean_wgt_le` · `selfConsistent_fdr_le_harmonic` · `benjamini_yekutieli` /
`benjamini_yekutieli_level` · `bh_uncorrected_bound` ·
`by_dominates_bonferroni` · `by_fdp_tail` · `harm_le_one_add_log` /
`log_le_harm` / `harm_thousand_le_eight` · `superuniform_grid` /
`not_superuniform_of_const` · `card_windowsThrough` / `pval_superuniform` /
`bhR_pv` / `bhRej_pv` / `bh_fdr_eq_harmonic` · `harmonic_factor_sharp` ·
`uncorrected_bh_exceeds_level` · `by_level_is_attained` ·
`hypotheses_satisfiable` · `Blocks` / `lift` / `card_lift` / `lift_inter` /
`stdBlocks` · `fdp_lift` · `region_screen_fdr_le` ·
`region_level_is_less_conservative` · `harmonic_gain_log`

**Calibration and prediction sets**
`Calibration.risk_decomposition` · `risk_eq_resolution_of_calibrated` ·
`PredictionSets.validity_is_free`

**Complexity**
`Complexity/biasThreshold_hard` — NP-hardness of the recalibration optimum,
proved from a verifier definition of NP through circuit satisfiability, a
verified Tseitin transformation, independent set and weighted linear ordering,
each link a total reduction with a machine-checked correctness proof and a
machine-checked polynomial size bound. Lean has no cost model; polynomiality is
carried by those size bounds on structurally simple, explicitly given functions
rather than proved as running time, and this is documented in the development.
`AUCCrossedMatchingLooseness.crossed_bound_loose_unbounded` — the companion
negative.

**Distributional design** (used by PREREG_8, and for the ensemble paper)
`DistributionVerdict.distributional_design_law` ·
`transportCost_line_eq_cdfL1` · `DistanceRealizability` ·
`DistanceCutEnsembles`

**Nothing is pending.** Five further files close every statement this work used
before it was proved:

- `DiscordantImbalance` — `nuPair_le_imbalanced` (`ν_pair ≤ κ·ε²/(1−ε)²`, no
  balance hypothesis and no bound on the noise rate), `one_le_kappa`,
  `kappa_eq_one_iff_balanced`, and the published `2ε²` bound as a corollary.
- `ConformalPValueBH` — `card_low_rank_le`, `pr_rank_le`,
  `conformalP_superuniform`, then the composition
  `conformal_selfConsistent_fdr_le_harmonic`, `conformal_benjamini_yekutieli`,
  `conformal_screen_fdr_le`, with
  `conformalP_dependent_of_shared_calibration` recording why arbitrary
  dependence is the right tool.
- `SeparationVsChain` — `pairSeparated_card_le_capacity` and
  `many_separations_short_chain` (a two-cluster instance certifying `2k²`
  ordered comparisons with no separated subfamily above two).
- `CapacityConfidence` — `tail_le_of_rate_le` (a tail bound uniform over the
  null region, sharper than sub-Gaussian in the small-rate regime),
  `capacity_or_rare_event`, and `capacity_at_95_confidence`: with 2,746 measured
  pairs, at least 160 disagreements has probability at most 1/20 under any true
  rate below 4%, and at 4% or above a benchmark certifiably orders at most 13
  methods. The test is one-sided by construction — a measurement can only
  lower-bound the noise rate, which is the direction that shrinks capacity.
- `UnresolvableCount` — `card_closePairs_lower`, `benchmark_unresolvable_count`,
  `unresolvable_count_117` (at least 798 of 6,786 comparisons undecidable).

Plus `AUCTargetMean` (`auc_target_strictMono_invariant`,
`auc_within_eq_pair_weighted_mean`, `weighted_ne_unweighted_instance`),
`crc_expected_risk_le`, and
`grouped_validity_underdetermines_element_coverage`.

Three items this list carried in an earlier draft —
`discordant_eq_flip_product`, `discordant_le_noise_sq` and
`pairwise_capacity_quadratic` — were proved first, and proving them changed two
of this paper's numbers (Note S2). **No claim in this paper now rests on a
citation or on an unproved statement.**

---

## Note S7 — The screen: arbitrary dependence, and the unit of testing

### What was missing

Two earlier parts of this development control the false discovery rate of a
proteome-scale screen for disordered regions, and each left the same hole. One
proves the Benjamini–Hochberg theorem in a **product** experiment — one
independent coordinate per candidate. The other drops independence entirely but
changes the currency: each candidate must supply an **e-value**, not a p-value.
A real screen reports p-values, and its candidates share a calibration run, a
model and a training set, so the dependence is neither absent nor
sign-constrained. Both files recorded the gap in the same words: *under
arbitrary dependence the Benjamini–Hochberg procedure needs the harmonic
correction, which is not proved here.*

### The theorem

Proved in the same finite, measure-theory-free setting (`Law Ω`: an arbitrary
probability distribution on a finite outcome space), assuming only
`Superuniform P X` — `P(X ≤ t) ≤ t` for every `t ≥ 0`, the validity of a **true
null's** p-value. Nothing is assumed about the non-nulls, the number of
candidates, or the joint law.

- `selfConsistent_fdr_le_harmonic` — any self-consistent step-up rule at level
  `q`: `E[FDP] ≤ q · H_m · |H₀| / m`.
- `benjamini_yekutieli`, `benjamini_yekutieli_level` — BH at the deflated level
  `α / H_m` controls the rate at `α`, whatever the dependence.
- `bh_uncorrected_bound` — uncorrected BH at level `q`: `E[FDP] ≤ q · H_m`.
- `by_dominates_bonferroni` — the corrected list still contains the Bonferroni
  list at the same overall level, so the correction never loses a discovery to
  the family-wise procedure.
- `harm_le_one_add_log`, `log_le_harm` — the price is
  `log(m+1) ≤ H_m ≤ 1 + log m`: logarithmic in the size of the screen, where
  Bonferroni's is linear. `harm_thousand_le_eight`: a factor under 8 for a
  thousand candidates, against Bonferroni's 1000.
- `superuniform_grid` / `not_superuniform_of_const` — the assumption is
  satisfiable and has content.

**The mechanism**, isolated as `wgt_decomp`, is why the joint law never enters.
Writing `R(ω)` for the reported list, the false discovery proportion is the
total weight `Σ_{i ∈ H₀} 1{i ∈ R}/|R|` (`fdp_eq_sum_wgt`), and the difficulty is
that `1/|R|` couples candidate `i` to every other. The identity

    1{i ∈ R}/|R| = Σ_{j=1}^{m} (1/j − 1/(j+1))·1{i ∈ R, |R| ≤ j}
                 + (1/(m+1))·1{i ∈ R}

holds on every single outcome, and the events on the right are **nested**, not
disjoint slices `{|R| = k}`. Self-consistency — every reported candidate has
`p_i ≤ |R|·q/m`, which the BH list satisfies (`bhList_selfConsistent`) — turns
each of them into a statement about candidate `i` alone,

    {i ∈ R, |R| ≤ j} ⊆ {p_i ≤ j·q/m},

whose probability validity alone bounds by `j·q/m`. Summing the telescoping
coefficients gives exactly `H_m` (`mean_wgt_le`). No property of the joint law is
used anywhere, which is why the result holds under arbitrary dependence.

### The factor is necessary, not conservative

For every screen size `m` and every level `q`, `DependentBHSharp.lean`
constructs the joint law that attains it. Outcomes are `none` together with the
pairs `(j, s)`; in outcome `(j, s)` the candidates of the cyclic window of length
`j+1` starting at `s` have p-value `(j+1)q/m` and everyone else has `1`, and the
outcome carries probability `q/((j+1)·m)`.

- The construction is balanced: each candidate lies in exactly `j+1` of the `m`
  windows of length `j+1` (`card_windowsThrough`), so it sees the value
  `(j+1)q/m` with probability exactly `q/m`, for every `j`. Every one of the `m`
  p-values is therefore valid (`pval_superuniform`) and all `m` hypotheses are
  true nulls.
- In outcome `(j, s)` the observed p-values are `j+1` copies of `(j+1)q/m`,
  which is exactly the step-up fixed point: BH stops at `j+1` and rejects
  precisely the window (`bhR_pv`, `bhRej_pv`). Every discovery is false.
- The discovery outcomes of window length `j+1` carry total mass `q/(j+1)`, so
  `E[FDP] = Σ_j q/j = q·H_m` (`bh_fdr_eq_harmonic`).

Consequences: the harmonic factor is attained (`harmonic_factor_sharp`);
uncorrected BH at level `q` strictly exceeds its nominal level from **two**
candidates on (`uncorrected_bh_exceeds_level`); and the corrected procedure sits
exactly at its own bound on the same instance (`by_level_is_attained`), so
`benjamini_yekutieli_level` cannot be improved either.
`hypotheses_satisfiable` supplies an admissible level for every `m ≥ 2`.

### Where the price should be paid

The factor depends on one thing only — how many hypotheses the screen states —
so the unit of testing is a design parameter, and disorder is a property of a
region.

- `Blocks n M b` partitions the `n` residues into `M` candidate regions of `b`
  residues; disjointness and common size are all that is used. `stdBlocks`
  supplies the concrete partition, so nothing is vacuous.
- `fdp_lift` — **the invariance**: reading a region-level report at residue
  level multiplies both the numerator and the denominator of the false
  discovery proportion by the region length, and it cancels. The two screens
  report the *same* FDP. This is exactly where region-level annotation matters:
  it is true because the truth and the report are both unions of whole regions.
- `region_screen_fdr_le` — so the region-level Benjamini–Yekutieli procedure at
  level `α/H_M` controls the **residue-level** false discovery rate at `α` under
  an arbitrary joint law.
- `region_level_is_less_conservative`, `harmonic_gain_log` — and it tests at a
  strictly more permissive threshold, with the saving bounded below by
  `H_n − H_M ≥ log b − 1`: one nat per factor in the region length.

### Instantiated on this paper's references

`results/caid3/region_screen.py`, job 30101613. Candidate regions are the
maximal same-label runs of each reference — the unit the annotation is actually
constant on — with a run broken by any unevaluated residue, which states more
hypotheses rather than fewer. Full table in **Table S15**.

| reference | n | M | mean region | H_n | H_M | saving | ≥ log b − 1 | threshold |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| CAID3 Disorder-PDB | 99,239 | 1,015 | 97.8 | 12.083 | 7.500 | 4.582 | 3.583 | ×1.61 |
| CAID3 Disorder-NOX | 99,977 | 576 | 173.6 | 12.090 | 6.934 | 5.156 | 4.157 | ×1.74 |
| CAID3 Binding | 28,263 | 165 | 171.3 | 10.827 | 5.686 | 5.140 | 4.143 | ×1.90 |
| CAID3 Binding-IDR | 7,741 | 157 | 49.3 | 9.532 | 5.637 | 3.895 | 2.898 | ×1.69 |
| CAID3 Linker | 20,498 | 105 | 195.2 | 10.505 | 5.236 | 5.269 | 4.274 | ×2.01 |
| CAID2 Disorder-PDB | 130,877 | 1,162 | 112.6 | 12.359 | 7.636 | 4.724 | 3.724 | ×1.62 |
| CAID2 Disorder-NOX | 160,802 | 623 | 258.1 | 12.565 | 7.013 | 5.553 | 4.553 | ×1.79 |
| CAID2 Binding | 67,169 | 219 | 306.7 | 11.692 | 5.969 | 5.724 | 4.726 | ×1.96 |
| CAID2 Linker | 37,150 | 124 | 299.6 | 11.100 | 5.402 | 5.698 | 4.702 | ×2.05 |

The proved floor `log b − 1` is met with room on every reference. A region-level
screen may test at a threshold 1.61× to 2.05× larger for the same residue-level
false discovery rate, and by `fdp_lift` it gives up nothing to do so.

### The screen, run

`results/caid3/conformal_screen.py` (job 30102082) runs the procedure on CAID3
Disorder-PDB: 304 usable chains, 98,938 evaluated residues, candidates are
consecutive blocks of `b` residues, p-values are split conformal against the
known-ordered blocks of 80% of chains, 50 random splits, medians reported.

| b | α | procedure | residues reported | realised residue FDP | power |
|---:|---:|---|---:|---:|---:|
| 100 | 0.10 | **region BY** (α/H_M) | **2,942** | 0.024 | **0.568** |
| 100 | 0.10 | residue BY (α/H_n) | 203 | 0.000 | 0.036 |
| 25 | 0.10 | **region BY** | 604 | 0.000 | 0.122 |
| 25 | 0.10 | residue BY | 201 | 0.000 | 0.027 |
| 10 | 0.10 | **region BY** | 562 | 0.000 | 0.098 |
| 10 | 0.10 | residue BY | 204 | 0.000 | 0.034 |

At 100-residue blocks the region-level screen reports **14.5× the residues** of
the residue-level screen at the same guaranteed rate, and 2.75–3.0× at 10 and 25.
Uncorrected region-level BH reports far more (4,225 at b=100) but realises a
false discovery proportion of 0.099 against a nominal 0.10 with no guarantee
under dependence — which is what the correction is being paid for.
Region-level Bonferroni reports nothing at any setting.

`fdp_lift` is checked rather than assumed: the region-level and residue-level
false discovery proportions of the same report agree to within 0.006 at b=10 and
0.077 at b=100. The gap is not zero because the truth is not exactly a union of
whole blocks in real data — a block is called disordered by majority — and the
size of the gap measures how far the theorem's hypothesis is from holding.

**A third constraint dominates both, and it is not in the theory.** A conformal
p-value cannot fall below `1/(n_cal + 1)`, and a step-up rule at level `α/H_m`
over `m` hypotheses applies thresholds as small as `α/(m·H_m)`. A screen is
therefore *arithmetically* incapable of a discovery unless

    n_cal + 1  ≥  m · H_m / α ,

whatever the predictor does. The requirement is **linear** in the number of
hypotheses, where the harmonic factor is logarithmic, so the unit of testing
sets the calibration budget far more strongly than it sets the correction.
Measured here, the region-level screen needs 13× fewer calibration units at
b=10, 36× at b=25, 80× at b=50 and 178× at b=100 than the residue-level screen.
Every configuration is still short of the requirement — by 23–60× for regions
and 38–77× for residues — and discoveries occur anyway only because many blocks
tie at the p-value floor. **This is the constraint that binds first in practice,
and a screen should be designed against it before the harmonic factor.**

### Scope

This is multiplicity control and it is orthogonal to calibration.
`Superuniform` is a statement about the *null law of the read-out*; if the
instrument's baseline rate is wrong, the p-values are not valid and every bound
above is void. The screen reported above is a demonstration on a public benchmark whose labels
are already known; **no biological discovery is claimed**, and its realised false
discovery proportion sits far below nominal, as arbitrary-dependence control
should.

---

## Note S19 — Is the bound tight, and does it transfer?

`card_le_benchCapacity` is a **worst-case** statement: it allows the annotation
errors to fall wherever they most damage a comparison. That is what makes the
converse an impossibility rather than a power calculation, and it is also the
one place the bound could be loose. A label flip on an item two methods *agree*
about moves both scores identically and cancels exactly; only flips on
disagreement items can change a comparison. So the budget that actually
threatens a comparison of methods A and B is

    nu_eff(A, B) = |{ambiguous} ∩ {A ≠ B}|        against    nu = |{ambiguous}|

and whether the worst case is reachable is an empirical question, not a
mathematical one:

    enrichment = P(A ≠ B | ambiguous) / P(A ≠ B)

### The measurement

`results/caid3/bound_tightness.py`, job 30342579. Ambiguity comes from MobiDB's
per-structure missing-residue calls; disagreement comes from the submitted CAID3
predictions, each binarised at its own median over the evaluated set so that
every method calls the same number of residues disordered and the comparison is
about *which* residues rather than how many. The two sources are independent —
unlike a confident-learning flag, which is defined by model disagreement and
would make the test circular.

86 methods aligned over 39,392 evaluated residues, of which 7,389 (18.8%) are
context-dependent; 3,653 method pairs.

| | median | mean | p10 | p90 |
|---|---:|---:|---:|---:|
| P(disagree) | 0.354 | 0.370 | 0.256 | 0.532 |
| P(disagree \| ambiguous) | 0.364 | 0.370 | 0.251 | 0.506 |
| **enrichment** | **0.992** | 1.009 | 0.880 | 1.175 |

46.3% of pairs exceed an enrichment of 1; **none exceeds 2**.

### It replicates on an independent round

`bound_tightness.py` on CAID2 (job 30342771): 61 methods, 58,831 evaluated
residues, 6,052 context-dependent (10.3%), 1,830 pairs.

| | CAID3 | CAID2 |
|---|---:|---:|
| methods / pairs | 86 / 3,653 | 61 / 1,830 |
| residues, ambiguity rate | 39,392 / 18.8% | 58,831 / 10.3% |
| P(disagree) | 0.354 | 0.388 |
| P(disagree \| ambiguous) | 0.364 | 0.372 |
| **enrichment (median)** | **0.992** | **0.963** |
| pairs with enrichment > 2 | 0 | 0 |

A different round, different proteins, different methods, and the same answer:
no enrichment. On CAID2 the enrichment is slightly *below* one, so the paired
budget there is smaller still.

### What it costs us

**Annotation ambiguity is not concentrated where methods disagree.** Errors fall
essentially independently of what any comparison turns on, which is the opposite
of what would make the worst case reachable. The paired budget is therefore
0.364× the marginal one, and:

| CAID3 Disorder-PDB, ε = 0.0801 | capacity |
|---|---:|
| marginal scores | **7** |
| paired, ×0.506 (10% of pairs are worse) | 13 |
| **paired, ×0.364 (median pair)** | **18** |
| paired, ×0.251 (10% of pairs are better) | 25 |

**CAID3 is over capacity by 6.5×, not 17×.** The theorem is unchanged and
correct — it bounds what a benchmark can certify about *marginal* scores, which
is what every entrant publishes — and 117 entrants against a paired capacity of
18 is still far over. But "over by a factor of seventeen" was the wrong number
to attach to a leaderboard comparison, which is paired by construction.

The refinement applies symmetrically: the pairwise item protocol goes from 51 to
138. The escape route is not diminished; both numbers move together.

### The principle, which is this paper's own

This is the paper's thesis applied one level up. The escape route says *score
pairs of items rather than items, because pairing cancels the error common to
both*. This says *compare pairs of methods paired on items rather than comparing
marginal scores*, for exactly the same reason. Two instances of one move.

### The scope condition

This is the honest answer to whether the findings transfer. **The theorem does;
its tightness does not.** The bound holds for every benchmark whose score is an
average over items, but how far the worst case overstates depends on a
dataset-specific quantity:

- **enrichment ≈ 1** — errors fall independently of the comparison, the paired
  budget is smaller by `P(disagree)`, and the marginal bound is loose by that
  factor. CAID3 is here.
- **enrichment ≫ 1** — ambiguous items are the items methods disagree about, the
  budget sits where comparisons are decided, and the marginal bound binds as
  stated.

Any benchmark with repeat annotations can measure this. Any benchmark without
them cannot, and should quote the marginal bound as the conservative one.
**Nothing licenses assuming CAID3's multiplier transfers**, and the ImageNet and
CIFAR figures in Table S17 are marginal capacities for that reason: their
adjudicated errors were selected by model disagreement, so they cannot be used
to estimate an enrichment without circularity.

---

## Table S9 — Coverage as a confound

What declining targets is worth, measured against a fully-covering yardstick.

| benchmark | method | declined | median length of declined | difficulty gap (AUC) |
|---|---|---:|---:|---:|
| Disorder-NOX | ESMDisPred-2PDB | 23 / 204 | 1,292 | **+0.118** |
| Linker | ESMDisPred-2PDB | 6 / 31 | 1,214 | **+0.125** |
| Disorder-PDB | SPOT-Disorder2 | 21 / 319 | 1,684 | +0.035 |

Median length across all CAID3 targets is 344. Long proteins are harder and they
are what gets declined. This is why the protocol makes coverage a gate (step 1)
and why every rank in this paper is reported twice — among all scored entrants
and among full-coverage entrants.

## Table S10 — Thresholded error counts

Each method's own binary call (column four of its `.caid` file), against the
reference, over every evaluated residue. 46 methods submitted a complete binary
column.

**Disorder-PDB** (99,239 evaluated residues)

| # | method | errors | rate | vs #1 |
|---:|---|---:|---:|---:|
| 1 | **DisorderNet-pbias** | **8,000** | 0.081 | — |
| 2 | PUNCH2 | 8,460 | 0.085 | +460 |
| 3 | DisorderNet-windowed | 8,481 | 0.085 | +481 |
| 4 | PUNCH2-Light | 8,716 | 0.088 | +716 |
| 5 | SETH-0 | 9,977 | 0.101 | +1,977 |

**Disorder-NOX** (99,977 evaluated residues)

| # | method | errors | rate | vs #1 |
|---:|---|---:|---:|---:|
| 1 | **DisorderNet-windowed** | **18,084** | 0.181 | — |
| 2 | DisorderNet-pbias | 18,983 | 0.190 | +899 |
| 3 | flDPnn2 | 20,803 | 0.208 | +2,719 |
| 4 | flDPlr | 22,382 | 0.224 | +4,298 |
| 5 | ESpritz-D | 22,426 | 0.224 | +4,342 |

**Thresholds are not comparable across methods.** Each entrant chose its own,
and a method optimising AUC rather than accuracy is penalised here through no
fault of its ranking. This is reported beside the AUC tables rather than instead
of them.

## Table S11 — Certification frontier

`LabelNoise.ranking_certified` turns a margin into a statement about which
method is better *in truth*: a margin above `2·ε·n` certifies the ranking at
annotation error rate `ε`. Because the criterion is monotone in `ε`, the whole
answer is a frontier and no point estimate of `ε` is needed.

| assumed annotation error | certified vs 45 others, Disorder-PDB | Disorder-NOX |
|---|---:|---:|
| 0.25% | 43 | 45 |
| 1% | 41 | 44 |
| **2%** | **39** | **43** |
| 3% | 34 | 34 |
| 5% | 21 | 9 |
| **8.01% (measured)** | **15** | **4** |

At the measured rate, **30 of 45** comparisons on Disorder-PDB and **41 of 45**
on Disorder-NOX are inside the annotation noise — not unresolved by this
analysis, but unresolvable.

The **breakdown rate** of a comparison, `margin/(2n)`, is the annotation error
rate that would have to be exceeded before that ranking could be an artefact.
For the closest comparison it is 0.232% on Disorder-PDB (PUNCH2, 460 residues)
and 0.450% on Disorder-NOX; median across the field, 4.78% and 3.71%.

## Table S12 — Data provenance

Reference FASTAs, from
`https://caid.idpcentral.org/assets/sections/challenge/static/references/3/`:

| file | md5 | targets | positives | negatives | unevaluated |
|---|---|---:|---:|---:|---:|
| `disorder_pdb.fasta` | `6feaff35263e7fd4a3f03640c23786fe` | 319 | 31,401 | 67,838 | 61,183 |
| `disorder_nox.fasta` | `e2056cccfe186ab9da8d1e15f1508bef` | 204 | 26,367 | 73,610 | 541 |
| `binding.fasta` | `3094881568be4105b0e9afc576a79558` | 52 | 2,991 | 25,272 | 0 |
| `binding_idr.fasta` | `4331f14502505e5dea58fcef0a29b2b7` | 52 | 2,991 | 4,750 | 20,522 |
| `linker.fasta` | `603e3055d5bed0d5af421a81123b252f` | 31 | 1,379 | 19,119 | 0 |

Prediction archive: `predictions.zip`, 76,480,806 bytes, 117 `.caid` files,
aggregate md5 of `md5sum *.caid` = `cdef7ae6dc3dd6ec8055aacdb14a54a7`.

**Published leaders reproduced through our pipeline** — the check that licenses
every other number:

| challenge | leader | computed | published |
|---|---|---:|---:|
| Disorder-PDB | PUNCH2 | 0.9552 | 0.955 |
| Disorder-NOX | ESMDisPred-2PDB | 0.8855 | 0.885 |
| Binding | DisoFLAG-PB | 0.7760 | 0.776 |
| Binding-IDR | bindEmbed21IDR-rawGeneral | 0.6407 | 0.641 |
| Linker | IPA-AF2-Linker | 0.8985 | 0.897 |

Use the dataset named **"CAID3 v3"**. The API also serves an entry named plainly
"CAID3" (185 proteins), which is a different, smaller set and does not reproduce
the published leaderboard.

## Table S13 — Job registry

| analysis | script | job | wall |
|---|---|---:|---|
| temporal holdout, build | `rockfish/build_temporal_holdout.py` | 30034161 | 1 min |
| temporal holdout, AlphaFold prefetch | — | 30034251 | 15 s |
| temporal holdout, evaluation | `rockfish/slurm/eval_temporal.sbatch` | 30034521 | 2 m 47 s |
| conformal guarantee | `rockfish/slurm/eval_temporal.sbatch` | 30035126 | 2 m 17 s |
| operating cost, whole field | `results/caid3/operating_cost.py` | 30037497 | — |
| registered endpoints, PREREG_7/_8/_9 | `results/caid3/registered_endpoints.py` | 30098857 | < 5 min |
| pairwise noise, corrected denominator | `results/caid3/relative_noise.py` | 30101641 | < 5 min |
| region screen instantiation | `results/caid3/region_screen.py` | 30101613 | < 5 min |
| pairwise leaderboard, re-run at ε_pair = 0.0100 | `results/caid3/pairwise_leaderboard.py` | 30101721 | < 5 min |
| registered variants on the temporal holdout | `rockfish/eval_temporal.py` | 30102022 | 14 min |
| the screen, run end to end | `results/caid3/conformal_screen.py` | 30102082 | < 5 min |
| capacity outside disorder | `results/caid3/benchmark_capacity_field.py` | local | seconds |
| bootstrap interval on ε and capacity | inline, `paper/figures/capacity_interval.json` | local | seconds |
| chirality A/B, treatment | `rockfish/train_multitask.py --chiral` | 30022469 | 11 h 08 m |
| chirality A/B, control | `rockfish/train_multitask.py` | 30022470 | 11 h 20 m |
| private trunk | `rockfish/train_multitask.py --private-attached` | 29922832 | 18 h 24 m |
| private trunk, evaluation | `rockfish/eval_caid3_official.py` | 30020129 | — |
| windowed training | `rockfish/train_multitask.py` | 29833016 | — |
| windowed evaluation | `rockfish/eval_caid3_official.py` | 29833018 | — |

Raw outputs are the JSON files under `paper/figures/`, each written by the job
named here and read directly by `paper/make_figures.py` and
`paper/make_supplementary.py`.
